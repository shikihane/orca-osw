interface:
  display_name: "OSW Agent Supervisor"
  short_description: "Coordinate Orca-backed coding agent sessions"
  default_prompt: "Use $osw to delegate and coordinate this task in the current worktree."
